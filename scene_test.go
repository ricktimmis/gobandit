package main

/* - commented out for now


import (
	"github.com/veandco/go-sdl2/sdl"
	"testing"
)

func TestdrawBackground(t *testing.T) {

	// It probably makes sense to break these functions out of main, into a
	// gamewindow function rather than repeating

	surface, err := window.GetSurface()
	if err != nil {
		panic(err)
	}


	// Test an existing BackgroundMusic image
	i := "./resources/backgrounds/dragon_of_the_north.jpg"

	r, err := sdl.CreateSoftwareRenderer(surface)
	if err != nil {
		panic(err)
	}
}
*/
